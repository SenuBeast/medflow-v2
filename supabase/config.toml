# No custom Edge Functions are configured for authentication.
# MedFlow authentication relies on Supabase Auth (Email/Password, Google OAuth, Native MFA).

[functions.admin-user-lifecycle]
verify_jwt = false
